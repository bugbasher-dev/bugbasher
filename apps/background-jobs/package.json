{
  "name": "background-jobs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf .nx node_modules dist",
    "dev": "npx trigger.dev@latest dev -c ../../trigger.config.ts"
  }
}
