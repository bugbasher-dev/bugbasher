{
  "name": "epic-stack-monorepo",
  "private": true,
  "packageManager": "npm@10.9.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "nx run-many -t build",
    "dev": "npm-run-all --parallel dev:proxy dev:all",
    "dev:all": "nx run-many -t dev -p app web cms admin studio docs --parallel=20",
    "dev:proxy": "node dev-proxy.js",
    "dev:web": "nx run-many -t dev -p web cms",
    "dev:app": "nx run app:dev",
    "dev:mobile": "nx run mobile:dev",
    "dev:trigger": "nx run background-jobs:dev",
    "lint": "nx run-many -t lint",
    "typecheck": "nx run-many -t typecheck",
    "test": "nx run-many -t test",
    "test:e2e:run": "nx run-many -t test:e2e:run",
    "test:e2e:install": "npx playwright install --with-deps chromium",
    "format": "prettier --write .",
    "db:setup": "cd packages/prisma && npm run setup",
    "db:generate": "cd packages/prisma && npm run db:generate",
    "db:push": "cd packages/prisma && npm run db:push",
    "db:migrate:deploy": "cd packages/prisma && npm run db:migrate:deploy",
    "db:seed": "cd packages/prisma && npm run db:seed",
    "db:migrate:dev": "cd packages/prisma && npm run db:migrate:dev",
    "docs:dev": "cd apps/docs && npx mint dev",
    "docs:build": "cd apps/docs && npx mint build"
  },
  "devDependencies": {
    "@epic-web/config": "^1.20.1",
    "@types/http-proxy": "^1.17.16",
    "ajv": "^8.17.1",
    "http-proxy": "^1.18.1",
    "npm-run-all": "^4.1.5",
    "nx": "21.6.5",
    "prettier": "^3.5.3",
    "prettier-plugin-sql": "^0.19.0",
    "prettier-plugin-tailwindcss": "^0.6.11",
    "react-test-renderer": "^19.1.1"
  },
  "engines": {
    "node": "22"
  },
  "prettier": "@epic-web/config/prettier",
  "volta": {
    "node": "22.15.0",
    "npm": "10.9.0"
  },
  "dependencies": {
    "motion": "^12.23.1",
    "react-image-crop": "^11.0.10",
    "zod": "^3.25.76"
  }
}
